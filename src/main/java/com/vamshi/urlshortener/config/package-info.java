/**
 * Spring {@code @Configuration} and {@code @Bean} definitions.
 *
 * <p>Contains infrastructure beans (JPA auditing, Redis, web MVC, security) that are
 * split into focused config classes rather than lumped into the main application class.
 */
package com.vamshi.urlshortener.config;
